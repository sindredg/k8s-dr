project_id          = "REPLACE_WITH_PROJECT_ID"
name_prefix         = "k8sdr-primary"
primary_zone        = "europe-north1-a"
# Choose an unused private range that does not overlap a future recovery subnet.
primary_subnet_cidr = "REPLACE_WITH_UNUSED_PRIVATE_CIDR"

# Use the same member as admin_member in infra/shared.
admin_member = "user:REPLACE_WITH_CLUSTER_ADMIN_EMAIL@example.com"

# Leave boot_image unset to use the tested image pinned in the module.
